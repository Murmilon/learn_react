let initialState = [
	{ id: 1, name: 'Dmitriy' },
	{ id: 2, name: 'Andrey' },
	{ id: 3, name: 'Sveta' },
];

const sidebarReducer = (state = initialState, action) => {
	return state;
}

export default sidebarReducer;